# Rust build output
/target

# napi-rs build artifacts (regenerated by `napi build`; the cross-platform
# prebuilt binaries are produced and published by the addon's own CI matrix).
*.node
/index.js
/index.d.ts
