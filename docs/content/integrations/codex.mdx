---
title: Codex
description: Use OpenKnowledge with OpenAI Codex.
---

OpenKnowledge plugs into Codex through MCP.

## Install

<McpInstall editor="Codex" />

## Open with AI

From the editor, **Open with AI ▸ Codex** dispatches the current file, folder, or project to Codex in one click. Two entry points: the sidebar's right-click menu (file / folder / empty space) and the macOS **File → Open with AI** menu. You can also hand the current doc to Codex from the bottom **Ask AI** composer.

Codex receives a short prompt asking it to open the target in OpenKnowledge's web preview. The agent then grounds the rest of the conversation through MCP. See [Editor → Open with AI](/docs/features/editor#open-with-ai) for the full UX.

## Verify

Open the project in Codex and ask:

<VerifyExec />

If you don't see the tool, restart Codex. If it's still missing and `ok init` reported your config was `left unchanged`, OpenKnowledge couldn't parse `~/.codex/config.toml` safely and left it untouched — fix the config and re-run `ok init`.
